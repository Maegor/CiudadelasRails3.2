<% content_for :head do%>

        <%= stylesheet_link_tag "menu", "parties" %>
        <%= javascript_include_tag "menu", "jquery.jscrollpane", "jquery.mousewheel" %>

<% end %>


<div id="parties_content" xmlns="http://www.w3.org/1999/html">
<h1>Partidas disponibles</h1>
<div  style="width:550px;">
<table  id="tblNeedsScrolling">

<thead>
  <tr>
    <th  >Nombre</th>
    <th>Jugadores(Max.)</th>
    <th></th>

  </tr>
</thead>
  <tbody>
  <% @party_list.each do |party| %>
      <tr>
        <td> <%=  party.name %> </td>
        <td align="center"> <%= party.current_player  %>   (<%= party.numplayer  %>)</td>



        <% if party.current_player <  party.numplayer %>
        <td class="right_side"> <%= link_to "Unirse", { :controller => :parties, :action => :join, :id => party.id} %></td>
        <% else %>
            <td class="right_side"> LLENA </td>
        <% end %>

       </tr>
  <% end %>
  </tbody>

</table>
</div>
  <p>
  <%= link_to "Atrás", root_path %>
</div>
